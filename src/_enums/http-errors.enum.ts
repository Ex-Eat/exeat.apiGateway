export enum HttpErrorsEnum {
	INVALID_TOKEN = 'Invalid Token.',
	TOKEN_EXPIRED_REFRESH_NEEDED = 'Token Expired. Refresh Needed',
	WRONG_APPLICATION = 'You tried to log in on the wrong application',
}
